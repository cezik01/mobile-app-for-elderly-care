import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    appInfo: {
        fontSize: 18,
        marginBottom: 90,
        fontStyle: 'italic',
        width: '95%',
    },
    buttonText: {
        fontSize: 16,
    },
    screen: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    helpContainer: {
        alignItems: 'center',
    },
    mic: {
        marginTop: 20,
    },
    helpText: {
        fontSize: 16,
        fontWeight: 'bold',
        textAlign: 'center',
    },
    voiceButtons: {
        marginTop: 30,
    }
});